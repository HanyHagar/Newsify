import 'package:flutter/material.dart';
import 'package:newsify/Widget/intro.dart';

class SplashScreen extends StatelessWidget {
  const SplashScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Intro();
  }
}
